{
  "asyncapi": "3.1.0",
  "info": {
    "title": "API",
    "version": "0.0.1"
  },
  "servers": {
    "streamer-finance-yahoo-com": {
      "host": "streamer.finance.yahoo.com",
      "protocol": "ws"
    }
  },
  "defaultContentType": "application/json",
  "channels": {
    "streamer-finance-yahoo-com": {
      "messages": {
        "pricing": {
          "payload": {
            "$ref": "#/components/schemas/Pricing"
          },
          "name": "pricing"
        },
        "subscribe": {
          "payload": {
            "$ref": "#/components/schemas/Subscribe"
          },
          "name": "subscribe"
        },
        "unsubscribe": {
          "payload": {
            "$ref": "#/components/schemas/Unsubscribe"
          },
          "name": "unsubscribe"
        }
      },
      "servers": [
        {
          "$ref": "#/servers/streamer-finance-yahoo-com"
        }
      ]
    }
  },
  "operations": {
    "receive": {
      "action": "receive",
      "channel": {
        "$ref": "#/channels/streamer-finance-yahoo-com"
      },
      "messages": [
        {
          "$ref": "#/channels/streamer-finance-yahoo-com/messages/pricing"
        }
      ]
    },
    "send": {
      "action": "send",
      "channel": {
        "$ref": "#/channels/streamer-finance-yahoo-com"
      },
      "messages": [
        {
          "$ref": "#/channels/streamer-finance-yahoo-com/messages/subscribe"
        },
        {
          "$ref": "#/channels/streamer-finance-yahoo-com/messages/unsubscribe"
        }
      ]
    }
  },
  "components": {
    "schemas": {
      "Pricing": {
        "type": "object",
        "required": [
          "type",
          "message"
        ],
        "properties": {
          "message": {
            "description": "Protobuf-encoded (base64 on the wire). No field is named — each line below is what the recorded samples show that field holding.\n\n  field 1 (length-delimited): a short string, e.g. \"AAPL\" — every value recorded here is one the client itself sent earlier in the session\n  field 2 (32-bit): a number, e.g. 317.1300048828125, 317.12908935546875\n  field 3 (varint): a number, e.g. 3575848608000, 3575848610000 (protobuf's varint wire type also allows a zigzag-encoded signed reading, which this would be: 1787924304000, 1787924305000 — the wire bytes alone do not say which is intended) — only ever grows from one sample to the next, like a running count\n  field 5 (length-delimited): a short string, e.g. \"NMS\"\n  field 6 (varint): a number, e.g. 8 (protobuf's varint wire type also allows a zigzag-encoded signed reading, which this would be: 4 — the wire bytes alone do not say which is intended) — the same value (8) in every sample; a small constant like this is often an enum member or a flag\n  field 7 (varint): a number, e.g. 1 (protobuf's varint wire type also allows a zigzag-encoded signed reading, which this would be: -1 — the wire bytes alone do not say which is intended) — the same value (1) in every sample; a small constant like this is often an enum member or a flag\n  field 8 (32-bit): a number, e.g. 0.8106104731559753, 0.8103194236755371\n  field 9 (varint): a number, e.g. 5104342, 5104372, 5105048 (protobuf's varint wire type also allows a zigzag-encoded signed reading, which this would be: 2552171, 2552186, 2552524 — the wire bytes alone do not say which is intended) — only ever grows from one sample to the next, like a running count\n  field 12 (32-bit): a number, e.g. 2.550018310546875, 2.549102783203125\n  field 22 (varint), present in 1 of 3 samples: a number, e.g. 100 (protobuf's varint wire type also allows a zigzag-encoded signed reading, which this would be: 50 — the wire bytes alone do not say which is intended)\n  field 27 (varint): a number, e.g. 4 (protobuf's varint wire type also allows a zigzag-encoded signed reading, which this would be: 2 — the wire bytes alone do not say which is intended) — the same value (4) in every sample; a small constant like this is often an enum member or a flag\n\nAcross every sample:\n  field 2 / field 8 ≈ 391.3 in every sample\n  field 2 / field 12 ≈ 124.4 in every sample\n  field 8 / field 12 ≈ 0.3179 in every sample",
            "type": "string",
            "examples": [
              "CgRBQVBMFaSQnkMYgMK+iYloKgNOTVMwCDgBRSuETz9I1sW3AmWAMyNAsAFk2AEE",
              "CgRBQVBMFaSQnkMYgMK+iYloKgNOTVMwCDgBRSuETz9I9MW3AmWAMyNA2AEE",
              "CgRBQVBMFYaQnkMY0NG+iYloKgNOTVMwCDgBRRhxTz9ImMu3AmWAJCNA2AEE"
            ],
            "contentEncoding": "base64"
          },
          "type": {
            "type": "string",
            "examples": [
              "pricing"
            ]
          }
        }
      },
      "Subscribe": {
        "type": "object",
        "required": [
          "subscribe"
        ],
        "properties": {
          "subscribe": {
            "type": "array",
            "items": {
              "type": "string",
              "examples": [
                "AAPL",
                "NVDA260828C00230000",
                "NVDA260828C00225000"
              ]
            }
          }
        }
      },
      "Unsubscribe": {
        "type": "object",
        "required": [
          "unsubscribe"
        ],
        "properties": {
          "unsubscribe": {
            "type": "array",
            "items": {
              "type": "string",
              "examples": [
                "AAPL",
                "NVDA260828C00230000",
                "NVDA260828C00225000"
              ]
            }
          }
        }
      }
    }
  }
}